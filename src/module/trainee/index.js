const traineeQuery = require('./query');
const traineeMutation = require('./mutation');
const traineeSubsciption = require('./subscription');
module.exports = {
    traineeQuery,
    traineeMutation,
    traineeSubsciption
}

